'use strict';

describe('myApp.Show Events module', function() {

  beforeEach(module('myApp.ShowEvents'));

  describe('ShowEvents controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var showEventsCtrl = $controller('ShowEventsCtrl');
      expect(showEventsCtrl).toBeDefined();
    }));

  });
});